{
  "name": "astrolabe",
  "version": "0.2.0-beta.1",
  "description": "Astrolabe beta: policy-driven OpenAI-compatible router for OpenClaw with safety gating and model escalation.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "docs:dev": "cd docs && npx mint dev"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "dotenv": "^16.6.1",
    "express": "^5.1.0"
  }
}
