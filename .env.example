# Required
OPENROUTER_API_KEY=your_openrouter_key_here
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Use the same value in your client Authorization header (Bearer token).
ASTROLABE_API_KEY=set_a_long_random_secret_here

# Network
PORT=3000

# Optional OpenRouter metadata headers
# OPENROUTER_SITE_URL=https://your-site.example
# OPENROUTER_APP_NAME=Astrolabe

# Runtime surfaces (defaults shown)
# ASTROLABE_RESPONSES_ENABLED=true                # true | false
# ASTROLABE_CHAT_COMPLETIONS_ENABLED=true         # true | false

# Routing modes (defaults shown)
# ASTROLABE_ROUTING_PROFILE=budget                # budget | balanced | quality
# ASTROLABE_COST_EFFICIENCY_MODE=strict           # strict | balanced | off
# ASTROLABE_DEFAULT_PROFILE=default               # default | strict-budget | safe-untrusted | low-latency | max-capability
# ASTROLABE_ALLOW_DIRECT_PREMIUM_MODELS=false     # true | false

# Safety controls (defaults shown)
# ASTROLABE_ENABLE_SAFETY_GATE=true               # true | false
# ASTROLABE_HIGH_STAKES_CONFIRM_MODE=prompt       # prompt | strict | off
# ASTROLABE_HIGH_STAKES_CONFIRM_TOKEN=confirm     # used when HIGH_STAKES_CONFIRM_MODE=strict
# ASTROLABE_ALLOW_HIGH_STAKES_BUDGET_FLOOR=false  # true | false

# Responses URL policy (defaults shown)
# ASTROLABE_RESPONSES_FILES_URL_ALLOWLIST=        # comma-separated hosts or origins
# ASTROLABE_RESPONSES_IMAGES_URL_ALLOWLIST=       # comma-separated hosts or origins
# ASTROLABE_RESPONSES_MAX_URL_PARTS=12

# Optional request rate limiting (defaults shown)
# ASTROLABE_RATE_LIMIT_ENABLED=false              # true | false
# ASTROLABE_RATE_LIMIT_WINDOW_MS=60000            # 1000-3600000
# ASTROLABE_RATE_LIMIT_MAX_REQUESTS=120           # 1-100000

# Classifier and self-check models (keys from src/manifests/index.js)
# ASTROLABE_CLASSIFIER_MODEL_KEY=nano
# ASTROLABE_SELF_CHECK_MODEL_KEY=mini

# Classifier context limits (clamped: messages 3-20, chars 600-12000)
# ASTROLABE_CONTEXT_MESSAGES=8
# ASTROLABE_CONTEXT_CHARS=2500

# Force every request to one model id (bypasses routing, classifier, and self-check escalation)
# ASTROLABE_FORCE_MODEL=
